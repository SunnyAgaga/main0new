# Copy to .env and fill in your own MongoDB Atlas (or local) connection string.
# Shared by the API server and the scripts/seed workflow.
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/wedplan
